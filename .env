# Copy to .env and fill in
OPENAI_API_KEY=sk-xxxxxx
# Optional overrides
# EMBEDDING_MODEL=text-embedding-3-small
# CHAT_MODEL=gpt-4o-mini

# ==== ContextForge MCP Gateway ===
MCP_ADMIN_USER=admin
#MCP_ADMIN_PASS=changeme
MCP_ADMIN_PASS=pass
# !!! Change this in real use
MCP_JWT_SECRET=replace-with-a-strong-secret

# ==== TSG Officer (LangGraph + Streamlit) ====
# Default is mock (no external API). Switch to openai to use OpenAI.
TSG_LLM_PROVIDER=mock
#TSG_OPENAI_MODEL=gpt-4o-mini
